
sudo: required
language: java

jdk:
  - openjdk8

script:
  - mvn package -DskipTests

before_deploy:
    - mkdir MiningBuild
    - find . -name '*.jar' -exec cp {} ./MiningBuild \;
    - cd /home/travis/build/semantic-conflicts/cf-java-client/MiningBuild
    - tar -zcvf result.tar.gz *
deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  file: result.tar.gz
  name: fetchjar-transformed-5f69c68fa0bf6e5450ae73b1cb0626895b3abdb3
  file_glob: true
  overwrite: false
  skip_cleanup: true
  on:
    all_branches: true 
            